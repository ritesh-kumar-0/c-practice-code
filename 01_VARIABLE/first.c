//Write a C program to calculate the area of a circle using the formula:
// Area = π × r × r
#include <stdio.h>

int main() {
int r = 6;
int area = 3.14*r*r;
printf("The are of circle is %D nad %f: ", r,area);
return 0;

}
