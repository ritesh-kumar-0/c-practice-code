//Write a C program to calculate the area of a circle
#include <stdio.h>

int main() {
    int r =  6;
    float area = 3.14 * r * r;
    printf("The area of circle with radius %d is %f", r, area); 
    return 0;
}

