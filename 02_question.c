#include <stdio.h>

int main() {
    int r =  6;
    float area = 3.14 * r * r;
    printf("The area of circle with radius %d is %f", r, area); 
    printf("The area of circle with radius %d is %f,r,");
    return 0;
}

